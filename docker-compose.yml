# Base Environment

version: "3.8"
services:
  friendo_site:
    build:
      context: .
    environment:
      - DEBUG
      - DJANGO_SECRET_KEY
      - POSTGRES_DB
      - POSTGRES_HOST
      - POSTGRES_PASSWORD
      - POSTGRES_USER
      - DEBUG_HOST